Main function is dehaze.m

The program uses Matlab optimtool (GlobalSearch) to solve DPA, but the cost of time is too much(about 30min).

We hope that more accurate and faster method to solve DPA could be used to improve the program in future.